# knowledge/ — curated technical memory

Stable, durable facts about the `.aim` format and this repo: design decisions
that have been made, naming/data-model rules, tooling notes. Edit in place;
keep it current. Rules → [`docs/README.md`](../README.md).

**Maintainer-authoritative:** changes here go through PR review like code
(external PRs may propose edits; the maintainer decides what is recorded).
Technical content only — no strategy or business reasoning in this public
repo.

Currently sparse by design: the repo is pre-v0.1 and the spec is an open
design question. Do not seed this folder with speculative spec content.
